version: '3.8'

services:
  manager:
    image: ghcr.io/metro-olografix/sede:dev
    restart: unless-stopped
    pull_policy: always
    networks:
      - "internal-apps"
    env_file: backend.env

networks:
  internal-apps:
    external: true